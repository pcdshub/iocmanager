#!/bin/bash
THIS_DIR="$(dirname "$(realpath "${BASH_SOURCE[0]}")")"
source "${THIS_DIR}"/default_env

"${IOCMAN_PY_BIN}"/pyuic5 -o ioc_ui.py ioc.ui
"${IOCMAN_PY_BIN}"/pyuic5 -o hostname_ui.py hostname.ui
"${IOCMAN_PY_BIN}"/pyuic5 -o auth_ui.py auth.ui
"${IOCMAN_PY_BIN}"/pyuic5 -o details_ui.py details.ui
"${IOCMAN_PY_BIN}"/pyuic5 -o commit_ui.py commit.ui
